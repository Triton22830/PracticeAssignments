function countingCharacters(stringToCount){
  // THe length property has been mentiond in the logging letter exercise in lesson 4
  console.log (stringToCount + "has" + stringToCount.length + "characters.");
}
